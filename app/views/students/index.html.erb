STUDENTTTTT TIMEEEEEEEE
<% @quizzes.each do |quiz| %>
  <div class="container">
    <h3><%= quiz.title %><h3>
        <%= link_to 'Take Quiz!', quiz_path(quiz), class: 'btn btn-primary btn-lg block' %>
  </div>
<% end %>
